<Window x:Class="DevTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical">
                <Button Content="Base64 Encode/Decode" Tag="base64" Click="Button_Click"/>
                <Button Content="XPath evaluation" Tag="xpath" Click="Button_Click"/>
            </StackPanel>
        </ScrollViewer>
        <GridSplitter Grid.Column="1"  HorizontalAlignment="Stretch" Width="5"/>
        <Frame x:Name="pageView" Grid.Column="2" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
